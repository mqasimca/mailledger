[build]
rustflags = ["-D", "warnings"]

[alias]
xtask = "run --package xtask --"
ci = "fmt --all -- --check && clippy --workspace -- -D warnings && test --workspace"

# Uncomment if you have mold linker installed (faster linking):
# [target.x86_64-unknown-linux-gnu]
# linker = "clang"
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]
